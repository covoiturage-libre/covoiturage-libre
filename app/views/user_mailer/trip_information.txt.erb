Bonjour <%= @trip.name %>


Information sur votre annonce :
<%= trip_title(@trip) %>

Lien de confirmation : <%= url_for confirm_trip_url(@trip.confirmation_token) %>

Lien de modification : <%= url_for edit_trip_url(@trip.edition_token) %>

Lien de suppression : <%= url_for delete_trip_url(@trip.deletion_token) %>
