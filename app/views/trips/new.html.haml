- @meta[:title] = 'Publier une annonce de covoiturage'
- @meta[:description] = 'Proposez vos places libres pour trouver des passagers, sur un site libre, gratuit et ouvert à tous.'

%h3
  - if back_trip_page?
    Créer le trajet retour
  - elsif trip_copy_page?
    Dupliquer le trajet
  - else
    Publier une annonce

= render 'form'